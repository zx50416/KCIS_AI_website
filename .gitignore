# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Raw crawl cache (may be large / not redistributable)
data/raw/**
!data/raw/.gitkeep

# User spec docs — keep local only; public repo uses AGENT_TECHNICAL_DESIGN_LOG.md
AGENT_IMPLEMENTATION_DATASET_PERMISSIONS_SPEC.md
REMOTE_DEPLOYMENT_REQUIREMENTS_康橋AI導航.md
ai工具label參考.md
康橋_AI應用導航網站_概念與初版方向.md

# Nested create-next-app git (repo lives at project root)
web/.git/

# Secrets
.env
.env.local
*.pem

# OS / IDE
.DS_Store
.idea/
.vscode/

# Model weights (never commit)
models/
*.gguf
*.safetensors

# Logo assets if added later (keep private)
assets/brand/

# Next.js
web/node_modules/
web/.next/
web/out/
web/.vercel/
